# CS2 Results Bot — краткий контекст

Обновлено: 21 августа 2026 года.

Этот файл — короткая точка входа для новой задачи Codex. Подробности нужно читать
по ссылкам только тогда, когда они относятся к запросу.

## Цель

Telegram-канал `@cs2_results` публикует расписание и результаты заметных матчей
CS2 без информационного шума. Ближайшая продуктовая цель — стабильный первый
канал и 1 000 подписчиков; сеть каналов не запускается до подтверждения качества
и спроса.

## Текущее состояние

- MVP работает в production на Yandex Cloud Functions, runtime Python 3.12.
- Последний production-релиз: PR #81; версия `d4eaif0a9n52ubj1ml7l`.
- Handler: `cs2bot.main.handler`.
- Результаты проверяются каждые 15 минут, расписание публикуется в 10:00, итог —
  в 23:00 по Москве.
- Основной источник — PandaScore. Liquipedia работает в shadow-режиме;
  production fallback пока выключен. Старые HLTV и BO3.gg адаптеры не участвуют
  в production-выборе.
- Доставка защищена per-channel claims и дедупликацией в Object Storage. После
  подтверждения Telegram claim фиксируется как `sent`, затем создаётся processed
  marker; незавершённый marker восстанавливается без повтора сообщения. При
  неопределённом исходе Telegram повтор и fallback запрещены, claim сохраняется
  для диагностики, а администратор получает алерт.
- Карточки результатов поддерживают 1–10 матчей, расписание — 1–20 матчей с
  разбиением на две страницы.
- Секреты хранятся в Yandex Lockbox. Функция не должна быть публичной.

## Текущий фокус

- Наблюдать production и исправлять только подтверждённые проблемы.
- Проверить качество турнирного радара и только затем решать вопрос о timer.
- Накопить 7–14 дней shadow-данных и сравнить полноту PandaScore с Liquipedia
  перед решением о fallback или полном переходе на Liquipedia как основной API.
- Настроить измерение источников подписчиков и эффективности публикаций.

Актуальная очередь: `BACKLOG.md`. Подробный operational snapshot:
`PROJECT_STATUS.md`.

## Неподвижные ограничения

- Не публиковать пустые выпуски и низкокачественные матчи ради частоты.
- Не смешивать разные источники в одной выдаче и не выдумывать отсутствующие
  данные.
- Не допускать повторной публикации при повторах invocation или переключении
  источников.
- Не раскрывать токены, ключи, секреты и приватный admin chat ID.
- PR, merge, deploy и Telegram-публикация требуют явного разрешения пользователя.

## Карта проекта

- `cs2bot/main.py` — Cloud Function handler, jobs и Telegram-доставка.
- `cs2bot/match_sources/` — источники, нормализация, фильтры и состояние доставки.
- `cs2bot/media_cards.py` — PNG-карточки.
- `cs2bot/analytics.py` — журнал продуктовой аналитики.
- `tier1_filter.json` — production-правила отбора.
- `docs/requirements.md` — действующие продуктовые требования.
- `docs/architecture.md` — компоненты и потоки данных.
- `docs/data-contract.md` — модели, fallback и дедупликация.
- `README.md` — конфигурация, локальный запуск и полный технический справочник.
- `PROJECT_STATUS.md` — production-снимок, проверки и известные риски.
