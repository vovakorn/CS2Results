[project]
name = "cs2-results-bot"
version = "0.1.0"
requires-python = ">=3.11"
description = "Telegram bot and normalized match-source layer for CS2 match results."

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
