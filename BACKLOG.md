# Backlog развития CS2 Results Bot

Обновлено: 10 сентября 2026 года.

Сюда попадают идеи до того, как они становятся задачами разработки. Статус:
`now` — делаем в текущем цикле, `next` — следующая подтверждённая очередь,
`later` — не начинать без отдельного решения. У каждого пункта указано, какую
проблему он решает, чтобы смысл задачи не зависел от старого контекста.

## Now

- [ ] Наблюдать первые автоматические выпуски турнирного радара: полноту сетки,
  отсутствие дублей и просмотры карточек через 24 часа.
  **Зачем:** подтвердить на реальных выпусках, что новый формат публикует полную
  и корректную сетку, не повторяется и интересен аудитории.

## Next

- [ ] Сохранять подтверждённый снимок итогов турнира после публикации финала и
  добавить целевой поиск/ручный выпуск по `tournament_parent` или названию.
  Снимок должен содержать источник, время подтверждения, все места и призовые;
  общий короткий список последних матчей для повторного выпуска не использовать.
  **Зачем:** уже завершённый турнир может исчезнуть из свежей выдачи API, хотя
  его финал и таблица ранее подтверждены. Снимок позволит безопасно повторить
  или отложенно выпустить итоги без выдумывания данных и без зависимости от
  текущего окна результатов.

- [ ] Сравнить PandaScore и Liquipedia по полноте результатов, расписаний,
  турниров, сеток, составов и карт; по итогам решить, оставлять Liquipedia
  fallback или переводить её в основной production-источник.
  **Зачем:** выбрать источник по фактическому качеству данных и сократить
  пропуски матчей без усложнения публикации смешиванием источников.

- [ ] Нормализовать больше форматов PandaScore brackets и выводить стадию,
  счёт и результат пар без догадок.
  **Зачем:** радар должен одинаково корректно показывать сетки разных турниров,
  не скрывать полезные данные и не подставлять предположения вместо фактов.

- [ ] Решить, нужны ли отдельные правила радара по турнирам и каналам после
  накопления первых автоматических выпусков.
  **Зачем:** не применять одну схему ко всем турнирам, если реальные данные
  покажут разный объём сеток, интерес аудитории или ограничения площадок.

### Архитектурные улучшения по итогам review

- [ ] Разделить `cs2bot/main.py` на тонкий router, независимые job-handlers и
  platform delivery adapters. Критерий готовности: handler только валидирует и
  маршрутизирует event, а schedule/results/digest/radar тестируются без импорта
  внутренностей других jobs.
  **Зачем:** уменьшить связанность jobs и платформ, чтобы изменения одного
  формата реже ломали другие и быстрее проверялись изолированными тестами.

- [ ] Вынести delivery state machine в отдельный модуль с едиными переходами
  `sending → attempting → sent|uncertain|released`, журналом причин и безопасной
  командой разрешения `attempting`/`uncertain`. Добавить state-aware cleanup и
  failure-injection тесты для падения до запроса, после принятия платформой и
  между `sent` и marker.
  **Зачем:** централизованно защищать Telegram, Instagram и Threads от дублей и
  дать понятный способ восстановить доставку после неоднозначного сбоя.

- [ ] Заменить полный scan `outbox/results/` на индексированную очередь по
  `next_attempt_at` и каналу с пагинацией, backoff и dead-letter/cancel policy
  для удалённых или отключённых каналов. Миграция должна читать старые items без
  остановки публикаций и подтверждать отсутствие starvation на 10 000 записей.
  **Зачем:** сохранить предсказуемое время доставки при росте очереди и не
  тратить каждый запуск на повторное чтение всех объектов.

- [ ] Убрать legacy-адаптеры HLTV/BO3.gg из production-архива после переноса
  нужных fixtures в контрактные тесты; закрепить в CI запрет импортов legacy из
  production selector и job-handlers.
  **Зачем:** уменьшить production-архив и поверхность сопровождения, сохранив
  полезные примеры данных и исключив случайный возврат старых источников.

- [ ] Добавить архитектурные проверки CI: границы импортов, статическую типизацию
  delivery API и лимит цикломатической сложности orchestration-функций. Пороговые
  значения зафиксировать после первого безопасного разбиения `main.py`.
  **Зачем:** не дать архитектуре снова срастись после разбиения `main.py` и
  ловить нарушение границ до merge.

### Качество production-release

- [ ] Документировать внешний автоматический release-процесс: где находится его
  конфигурация, чем он запускается, где меняются обычные переменные и ссылки на
  секреты, как проверить candidate, promote, smoke и rollback.
  **Зачем:** сейчас репозиторий содержит только workflow проверок
  `.github/workflows/tests.yml`; без карты внешней автоматизации невозможно
  уверенно изменить production-настройку или восстановить release после сбоя.

## Later

- [x] Итоговая таблица турнира по VRS: изменение очков и позиций команд в
  рейтинге. Реализован отдельный альбом с дельтами VRS-очков и мест, который
  превращает финальные результаты турнира в понятный пост о влиянии события на
  мировой рейтинг.
  **Зачем:** показать не только итоговые места, но и влияние турнира на рейтинг
  команд — отдельный информационный повод после завершения события.

- [ ] Расширить контекст матча: очные встречи, изменения составов, стадия
  турнира и один объясняющий инсайт. Сначала спроектировать cache и лимиты API.
  **Зачем:** объяснять, почему матч важен и чего ждать зрителю, не превращая
  канал в поток сырых статистических данных и не превышая лимиты источников.

- [ ] Продуктовая аналитика: таблица постов с форматом, турниром, временем,
  просмотрами за 1/24 часа, подписками и отписками.
  **Зачем:** видеть, какие форматы и темы действительно дают просмотры и рост,
  а какие занимают место без результата.

- [ ] Источники подписок: отдельные invite links для посевов, партнёров и
  соцсетей; хранить mapping кампании и дату запуска.
  **Зачем:** отличать органический рост от конкретных размещений и сравнивать
  каналы привлечения по фактическим подпискам.

- [ ] Еженедельный отчёт: прирост подписчиков, median views 24h, охват,
  ER по формату и эффективность источников.
  **Зачем:** регулярно принимать решения о контенте и продвижении по одним и
  тем же метрикам, а не по отдельным удачным или неудачным постам.

## Снято с очереди

- **Диагностика Tier-1 фильтра за 7–14 дней.** Снята 6 сентября 2026 года:
  текущая работа фильтра устраивает. Задача была нужна, чтобы проверить качество
  tiers `S`/`A` и решить, применять ли их к результатам. Действующие правила
  отбора сохраняются; решение о применении tiers к результатам не принято.

- **Закрепление `INSTAGRAM_EXPECTED_USER_ID`.** Отложено 6 сентября 2026 года:
  аккаунт Instagram менять не планируется. Задача защищает переавторизацию и
  callbacks Meta на отзыв доступа и удаление данных; вернуться к ней только при
  появлении одного из этих сценариев.

## Завершено

- [x] Реализовать универсальную итоговую таблицу турнира после финала: все
  места и призовые публикуются отдельным альбомом в Telegram и Instagram;
  шаблон получил медальные акценты и единую обводку шапки (PR #110,
  production 9 сентября 2026 года). Логотип конкретного турнира намеренно не
  используется.
  **Зачем:** дать подписчику один сохраняемый итог турнира вместо поиска мест и
  призовых по нескольким публикациям и внешним сайтам.

- [x] Восстановить отдельный пост расписания перед контекстом: PR #106,
  production-релиз 6 сентября 2026 года, проверки пройдены.
  **Зачем:** вернуть ожидаемую структуру утреннего выпуска, в которой расписание
  публикуется отдельным постом до дополнительного контекста.

- [x] Добавить раннюю проверку размера ZIP, обязательный приватный package
  bucket для архива больше 3,5 МБ и lifecycle для старых архивов.
  **Зачем:** не начинать заведомо неуспешный deploy из-за лимита Cloud Functions
  и не накапливать бессрочно release-архивы.

- [x] Проверить новый release-процесс на реальном облачном выпуске: создать и
  проверить candidate до изменения production, затем выполнить promote и
  проверить теги, smoke и готовность rollback.
  **Зачем:** доказать, что production переключается только на уже проверенную
  версию и может быть возвращён на предыдущую при ошибке.

- [x] Разделить `candidate` и `promote`, сохраняя release manifest с Git SHA,
  SHA-256 архива и ID предыдущей и новой версий.
  **Зачем:** гарантировать, что promote выпускает тот же проверенный immutable
  архив, а не результат новой сборки.

- [x] Автоматизировать post-deploy smoke: проверку production-тега, пяти timer
  trigger, production `dry_run=true`, startup-ошибок и rollback при неуспехе.
  **Зачем:** обнаруживать сломанный выпуск сразу после переключения и возвращать
  рабочую версию без повторной сборки.

- [x] Автоматизировать release вне репозитория.
  **Зачем:** убрать повторяемые ручные действия и снизить риск ошибки при
  candidate, promote, smoke и rollback. Расположение конфигурации вынесено в
  отдельную задачу, потому что в репозитории его сейчас нет.

## Как вести

- Каждая новая идея сначала получает короткий пункт и обязательное поле
  **«Зачем»**, без обязательства реализовывать её.
- Перед переносом в `Now` фиксируем цель, метрику успеха и существенный риск.
- Завершённые пункты остаются здесь краткой историей; фактическое состояние
  production после релиза также фиксируется в `PROJECT_STATUS.md`.
