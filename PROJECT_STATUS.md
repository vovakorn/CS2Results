# CS2 Results Bot — production-состояние

Обновлено: 10 сентября 2026 года.

Этот файл содержит подробный operational snapshot. Для обычной задачи достаточно
`PROJECT_CONTEXT.md`; этот документ нужен для релиза, инфраструктуры, диагностики
production и сверки фактического состояния. Текущий commit всегда проверяется
через Git и здесь не дублируется.

## Production

- MVP работает в Yandex Cloud Functions.
- Основная ветка GitHub: `main`.
- Runtime: Python 3.12.
- Handler: `cs2bot.main.handler`.
- Function ID: `d4e6e13rlrl7go01m2q2` (`cs2results`).
- Yandex Cloud folder ID: `b1g5j8hk4gjas2vpvgqr`.
- Последний production-деплой: 9 сентября 2026, версия `d4evr3lci669potqgrl0`
  (Git merge `0760632`, доработка карточек итогов турнира из PR #110);
  таймеры вызывают
  тег `production`. Предыдущая версия `d4e5rsmh5u8jm6t8fc5l`
  закреплена тегом `rollback`.
- Release-архив хранится в приватном unversioned bucket
  `cs2results-function-packages-b1g5j8hk4gjas2vpvgqr`; lifecycle удаляет только
  `function-packages/` через 30 дней. Release manifest хранится отдельно.
- В production включён флаг `ENABLE_LIQUIPEDIA_FINAL_CARDS=1`; Liquipedia fallback
  остаётся выключен.
- Пять timer trigger вызывают тег `production`, а не `$latest`.
- Полная release-проверка: 391 тест и сборка архива основной функции.
  Candidate и production smoke прошли: `200`,
  `dry_run=true`, без startup-ошибок и публикаций.
- GitHub Actions проверяет зависимости, безопасность, компиляцию, pytest и
  сборку архива. Оркестрация автоматического release-цикла находится вне
  репозитория.

## Расписание jobs

- `results` — каждые 15 минут.
- `results retry_only` — каждые 5 минут, без запроса источников матчей.
- `schedule` — ежедневно в 10:00 по Москве.
- `digest` — ежедневно в 23:00 по Москве.
- `radar_discovery` — ежедневно в 12:00 по Москве; выбирает Tier-1 турниры
  следующего дня, проверяет фактический первый матч турнира и публикует только
  при подтверждённых парах сетки; повторная публикация по тому же tournament ID
  и каналу блокируется стабильным ключом.
- `radar` — ручной выпуск выбранного турнира.
- `analytics` — ручные или специально настроенные операции аналитики.

Пустые выпуски не публикуются. Подробные продуктовые правила находятся в
`docs/requirements.md`.

## Источники

- PandaScore — основной production-источник завершённых и предстоящих матчей.
- PandaScore tournament tier `S`/`A` участвует в production-отборе утреннего
  расписания после исключений qualifier, showmatch, academy, youth и junior.
  Для завершённых результатов tier остаётся диагностикой, а решение принимает
  строгий Tier-1 LAN-фильтр.
- LiquipediaDB подключена для fail-open shadow-сравнения; production fallback
  остаётся выключен до решения по итогам наблюдений.
- Официальный VRS-адаптер читает versioned `live/{year}/standings_global_*.md`
  из публичного репозитория Valve, сохраняет дату снимка и blob SHA, а при
  неполном наборе команд блокирует VRS-альбом. Влияние формулируется как
  изменение после турнира, поскольку между недельными снимками могут быть
  другие события.
- Источники не смешиваются внутри одной выдачи.

## Хранилище и безопасность

- Токены и ключи находятся в Yandex Lockbox.
- Cloud Function не должна быть публичной.
- Object Storage хранит claims, processed markers и analytics journal.
- Durable outbox хранит нормализованный результат до подтверждённой доставки;
  очередь отдаёт приоритет ещё не обработанным и наименее недавно
  обрабатывавшимся элементам.
- Per-channel claim создаётся атомарно; после подтверждённой Telegram-доставки он
  фиксируется как `sent`, затем создаётся processed marker. Если запись marker
  прервана, следующий запуск восстанавливает его без повтора сообщения.
- Истёкшие claims можно безопасно перехватывать; ETag сравнивается совместимо с
  S3-реализациями, возвращающими значение без кавычек.
- Пользовательские metadata Object Storage читаются регистронезависимо, поскольку
  Yandex может возвращать `Delivery-State` и `Expires-At` с изменённым регистром.
- Перед релизом 1 сентября реальный bucket подтвердил conditional-write
  семантику: из десяти параллельных `If-None-Match: *` claim успешен ровно один,
  а повторная запись со старым ETag отклоняется.
- Cross-provider fingerprint защищает от дублей при переключении источников.
- Критические ошибки отправляются администратору с cooldown; приватный chat ID не
  документируется.
- Внешние логотипы ограничены разрешёнными HTTPS-хостами, типом, размером,
  разрешением, timeout и redirects.

## Проверенные возможности

- Tier-1 фильтрация по турнирам, стадиям, командам и исключениям из
  `tier1_filter.json`.
- Расписание, результаты и вечерний итог с Telegram-спойлерами.
- Карточки результатов на 1–10 матчей и соцсетевого расписания на 1–20 матчей.
- Telegram-обложки контекста по турниру и формату; короткий одиночный контекст
  прикрепляется к обложке, длинный безопасно разбивается между матчами.
- Две хронологические страницы соцсетевого расписания на 11–20 матчей.
- Официальные логотипы PandaScore, placeholder с инициалами и контрастные
  подложки.
- Текстовый fallback при определённом отклонении карточки Telegram API; при
  неизвестном исходе доставки повтор и fallback не выполняются, чтобы не создать дубль.
- `ConnectTimeout` до установления TCP-соединения считается определённым
  отказом: для PNG результата выполняются до двух попыток по 10 секунд,
  затем текст пробуется в том же invocation. Неудача текстового соединения
  оставляет outbox пятиминутному `retry_only` trigger.
- После photo `ConnectTimeout` результаты на один час переходят в text-only режим,
  чтобы загрузка PNG не расходовала бюджет остальных публикаций.
- Контекст всех отобранных матчей: форма и подтверждённые очные встречи без
  прогноза, сгруппированные по турнирам.
- Турнирный радар: до 24 подтверждённых пар PandaScore, по четыре пары на
  квадратную PNG-карточку с нумерацией страниц; таблица положения не публикуется.
- VRS-альбом: отдельный outbox и content UID, две соседние колонки изменения
  очков и мест, snapshots до/после в Object Storage; production включается
  только после настройки `ENABLE_VRS=1`.
- Radar discovery и один контролируемый тестовый пост карточек сетки успешно
  доставлены; dry-run без подтверждённых пар корректно не создаёт публикацию.
- Analytics journal для событий публикаций, снимков подписчиков, invite links и
  ручного импорта метрик.
- Отдельный OAuth handler для Instagram и Threads развёрнут в production как
  функция `cs2-social-oauth`, версия `d4es90gretnlskkega7g`. Запросы к Meta идут
  через Xray-конфиг из Lockbox, отдельный от Telegram proxy; запросы к Yandex
  Lockbox остаются прямыми. Threads OAuth завершён и создал активную версию
  Lockbox; Threads `USER_ID` закреплён в функции как
  `THREADS_EXPECTED_USER_ID` для проверки deauthorize и data-deletion callbacks.
  Закрепление Instagram expected user ID отложено до переавторизации аккаунта
  или необходимости обработать callbacks Meta.
- Telegram-прокси подключён отдельным Lockbox-секретом к основной production-функции;
  dry-run, запрос `getChatMemberCount` и один изолированный тестовый PNG-пост
  выполнены успешно.
- Instagram publishing включён в production 30 августа 2026: основная функция
  использует Xray из Lockbox, OAuth-токен из отдельного Lockbox-секрета и
  публичный bucket `cs2results-instagram-media` только для карточек. Расписание
  и digest имеют отдельные content-claims, результаты — отдельный durable
  outbox-канал `instagram`; Telegram proxy и его доставка не изменялись.
- Threads publisher с отдельными content-claims и durable outbox-каналом
  `threads` развёрнут в основной production-функции 31 августа 2026, версия
  `d4e6r3is9gugc6j6t2jt`, но выключен `ENABLE_THREADS_PUBLISHING=0`. Production
  dry-run завершился `200`; реальных Threads-постов этот релиз не создавал.
  Пользователь включил нативный Instagram → Threads cross-posting для одинаковых
  карточек; его работу с Instagram Graph API нужно подтвердить ближайшим штатным
  Instagram-выпуском. Неуспешный приватный Instagram test invocation (`502`) не
  повторялся, чтобы исключить дубль.
- Новый release-процесс применён 6 сентября: deploy-скрипт копирует environment
  variables и Lockbox bindings из production, candidate собирается один раз,
  проходит dry-run и анализ startup-логов и сохраняется в release manifest.
  Отдельный `promote` переключает тег без повторной сборки, выполняет production
  smoke и при его ошибке проверяемо откатывается. Для ZIP больше 3,5 МБ
  обязателен проверенный приватный package bucket с lifecycle. Manifest связывает
  Git SHA `1e526b4`, SHA-256 архива
  `f02d2587142eda26e79c9d92a0155d5009463ca60ea984f673f9adfabeee7386` и обе
  версии. Ручной rollback в production намеренно не запускался после успешного
  smoke; готовность команды покрыта failure-injection тестами и тегом `rollback`.

## Релиз 6 сентября 2026 — восстановление расписания

- В Telegram восстановлена отдельная карточка с парами и временем начала;
  контекст с прежней обложкой выходит после подтверждения расписания. Ошибка
  контекста не вызывает повтор основной публикации.
- PR #106 слит после успешного CI на Python 3.11 и 3.12. Полная проверка
  текущей production-базы: 401 тест; loopback-тест отдельно прошёл вне sandbox.
- Проверены изображения на 1, 4, 10 и 16 матчей, длинные названия,
  отсутствующие и контрастные логотипы, квадратный формат 1080 × 1080.
- Candidate `d4e08q1k0864mn0rqpv2` из Git `39deb66` прошёл проверку
  запуска; после promote production smoke вернул `200` без startup-ошибок.
  Все пять таймеров вызывают `production`; предыдущая версия сохранена для отката.
- Дополнительный production dry-run расписания вернул `200`: получено 9
  upcoming-матчей, Tier-1 отбор прошли 0. Пустой выпуск не публиковался;
  отдельный реальный тестовый пост не требовался.
- Релиз собран поверх фактически работающей локальной production-базы.
  В PR вошло только исправление расписания; существовавшие локальные коммиты
  процесса деплоя сохранены отдельно от GitHub main.

## Релиз 6 сентября 2026

- Создан отдельный private package bucket с 30-дневным lifecycle для
  `function-packages/`; media и state bucket не менялись.
- Candidate `d4e8lrfg9ihbfjsct4ob` создан из уже собранного архива размером
  16 339 780 байт, прошёл dry-run и анализ startup-логов.
- После явного подтверждения `promote` переключил production на candidate без
  повторной сборки. Все пять timer trigger остались закреплены за
  `production`; post-deploy smoke прошёл.

## Релиз 4 сентября 2026

- Проведён review изменений за последние две недели и устранён критический риск
  повторной публикации: перед внешним запросом claim становится
  неперехватываемым `attempting`; неоднозначный исход фиксируется как
  `uncertain`, а подтверждение — как `sent`.
- Если платформа подтвердила публикацию, но запись `sent` окончательно не удалась,
  состояние `attempting` остаётся fail-closed и не открывает повтор после TTL.
- Instagram HTTP 5xx, невалидный JSON/data и отсутствующий publish ID теперь
  считаются неоднозначным исходом так же, как в Threads.
- У неоднозначных результатов outbox удаляется; automatic retry отключён до
  ручной проверки платформы. Уже обработанные Instagram/Threads items очищаются
  при повторном обнаружении.
- Outbox сканирует все страницы Object Storage до сортировки и применения limit,
  поэтому первые 200 лексикографических ключей не могут блокировать остальные.
- Исправлена подпись карточки расписания; README, требования, архитектура,
  data contract, lifecycle и deploy-инструкция синхронизированы с кодом.
- Полный локальный набор: 366 тестов, все прошли. PR #105 слит в
  `main`; версия `d4efef6caa8g4qd6u7hc` развёрнута через приватный package bucket,
  candidate и production dry-run прошли. Реальный тестовый пост не потребовался.

## Что наблюдать

- Пропущенные и ошибочно включённые Tier-1 матчи.
- Появление и корректное завершение объектов `claims/` и `processed/`.
- Доставку технических алертов, состояние `sent` и отсутствие дублей после повторных invocation.
- Карточки в реальные дни с 1, 4, 10 и 16 матчами.
- Качество PandaScore tier diagnostics и Liquipedia shadow-сопоставления в течение
  7–14 дней.
- Содержательность контекста расписания и турнирного радара.
- Расходы и ошибки Cloud Functions, Object Storage и Logging.
- Источники подписчиков, просмотры за 24 часа и отписки после подключения
  продуктовой аналитики.

## Известные ограничения

- PandaScore может не предоставлять полную статистику карт и игроков.
- Liquipedia fallback не активирован до завершения shadow-проверки.
- Классификация Tier-1 зависит от поддерживаемого конфига турниров и стадий.
- Расписание ограничено двадцатью матчами и двумя карточками.
- PandaScore может не опубликовать сетку до старта турнира; в этом случае
  автоматический радар намеренно не выходит.
- Fail-closed протокол намеренно блокирует автоматический повтор после
  сбоя в состоянии `attempting`: до появления ручного resolver такой claim нужно
  сверять с платформой и разрешать операционно.
- Полноценная аналитика источников подписчиков ещё не настроена.

## Безопасный release-процесс

1. Проверить итоговый diff и отсутствие секретов.
2. Запустить полный набор тестов один раз.
3. Создать один PR и дождаться CI.
4. После явного разрешения пользователя выполнить merge.
5. Автоматический release-процесс вне репозитория выполняет `check`, candidate,
   проверку release manifest, promote и production smoke; при неуспехе выполняется rollback.
6. Провести один контролируемый тестовый пост, только если он нужен.
7. Обновить этот файл и `PROJECT_CONTEXT.md`, если production-состояние изменилось.

Команды и подробности: `docs/yandex-cloud-deploy.md`. Актуальная продуктовая
очередь: `BACKLOG.md`.
